# Merged

The `npm-package` branch is now merged to
[master](https://github.com/merkatorgis/docker4gis). The `dg` command,
pipelines, and automatic deploy are now generally available.

There's a [pre-npm](https://github.com/merkatorgis/docker4gis/tree/pre-npm)
branch with the state from before the merge.
