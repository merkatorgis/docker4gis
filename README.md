# Merged

The `npm-package` branch is now merged to [master](/). The `dg` command,
pipelines, and automatic deploy are now generally available.

There's a [pre-npm](/tree/pre-npm) branch with the state from before the merge.
