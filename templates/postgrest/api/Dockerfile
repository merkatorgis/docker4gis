FROM docker4gis/postgrest:229

# ENV PGRST_DB_POOL=100
# ENV PGRST_DB_EXTRA_SEARCH_PATH=public
# ENV PGRST_SERVER_HOST=*4
# ENV PGRST_SECRET_IS_BASE64=false
# ENV PGRST_JWT_AUD=
# ENV PGRST_MAX_ROWS=
# ENV PGRST_ROLE_CLAIM_KEY=".role"
# ENV PGRST_ROOT_SPEC=
# ENV PGRST_RAW_MEDIA_TYPES=
