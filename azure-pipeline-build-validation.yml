trigger:
- none

pr:
# NOTE: "npm-package" -> "master" after merge
- npm-package

steps:
# Build the package image.
- bash: |
    cd base/package
    export DOCKER_BASE=..
    ./build.sh
  displayName: 'build package image'
